sequenceDiagram
    participant browser
    participant server

    Note right of browser: User types a new note and clicks Save

    browser->>browser: JavaScript captures form submission
    Note right of browser: Prevents default page reload

    browser->>browser: Adds new note to local array
    browser->>browser: redrawNotes() updates the displayed list immediately

    browser->>server: POST /exampleapp/new_note_spa
    activate server
    Note right of server: Server saves the new note to database
    server-->>browser: 201 Created (confirmation)
    deactivate server

    Note right of browser: Browser shows updated notes list without reloading

